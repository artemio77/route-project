apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bootstrap-cluster-application
spec:
  destination:
    namespace: bootstrap-cluster
    server: 'https://kubernetes.default.svc'
  source:
    path: argocd/application
    repoURL: 'https://gitlab.com/route-project/deploy-config/'
    targetRevision: HEAD
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true